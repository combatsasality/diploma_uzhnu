import { Flex } from "antd";
import { Text } from "components";
import { FC } from "react";

export const FileViewer: FC = () => {
  return (
    <Flex>
      <Text preset="title">Test</Text>
    </Flex>
  );
};
